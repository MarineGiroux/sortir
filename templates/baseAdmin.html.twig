{% extends 'base.html.twig' %}

{% block body %}

<div class="d-flex flex-column">
    <header>
        {% include('navbar.html.twig') %}

    </header>

    <main class="flex-grow-1">


        {% if is_granted('ROLE_ADMIN') %}
        <div class="container-fluid">
            <div class="row">
                <nav class="col-md-2 d-none d-md-block bg-transparent sidebar navbar-vertical border border-dark ">
                    <div class="sidebar-sticky">
                        <h4>Dashboard</h4>
                        <ul class="nav flex-column">

                            <li class="nav-item"><a class="nav-link active" href="{{ path('app_dashboard_users') }}">
                                    Afficher Utilisateur</a></li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_dashboard-sortie') }}"> Afficher sorties</a></li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_villes_list') }}"> Gérer les villes</a></li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_sites') }}"> Gérer les sites</a></li>
                        </ul>
                    </div>
                </nav>

                <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-md-4">
                    {% endif %}

                    {% block body2 %}

                    {% endblock %}


                    {% if is_granted('ROLE_ADMIN') %}

                </main>
            </div>
        </div>

        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>
        {% endif %}

    </main>

</div>
{% endblock %}